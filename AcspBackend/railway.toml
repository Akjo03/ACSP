[build]
builder = "DOCKERFILE"
dockerfilePath = "/AcspBackend/Dockerfile"

[deploy]
healthcheckPath = "/actuator/health/liveness"
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5